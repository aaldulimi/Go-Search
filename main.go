package main

func main() {
	API()
	// query := "Blackberry ends phone support"
	// filename := "nytimes.json"

	// index := BuildIndex(filename)
	// results := Search(query, 10, index, filename)
	// PrintResults(results, true, false, false, false)

}
